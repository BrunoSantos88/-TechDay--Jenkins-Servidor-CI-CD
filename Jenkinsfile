pipeline {
  agent any
  tools { 
        maven 'Maven 3.5.2'  
    }

stage('Setting the variables values') {
    steps {
         sh '''
            #!/bin/bash
            echo "hello world"
         '''
    }
}

}